<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all HTTP for development (emulator + physical device on same WiFi). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
